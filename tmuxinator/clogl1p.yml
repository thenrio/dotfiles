name: clogl1p
socket_name: clogl1p
root: .
windows:
  - jam:
      layout: main-horizontal
      panes:
        - opt: 
          - ssh frclogrfidl1p
          - cd /opt/jam
        - log:
          - ssh frclogrfidl1p
          - cd /var/log/jam
          - tail -f jam.log
  - pickle:
      layout: main-horizontal
      panes:
        - tk:
          - ssh frclogrfidl1p
          - cd /var/log/pickle
          - tail -f tk.log
        - tunnel:
          - ssh frclogrfidl1p
          - cd /var/log/pickle
          - tail -f tunnel.log

name: deploy
root: ~/src/beaumanoir/deploy 
windows:
  - install:
      layout: main-horizontal
      panes:
        - 
        - . /usr/share/virtualenvwrapper/virtualenvwrapper.sh && workon deploy
  - repl:
       - 
  - run:
       - 
  - eval:
       - 

name: deploy
root: ~/src/beaumanoir/deploy 
windows:
  - install:
      layout: main-horizontal
      panes:
        - vim .
        - workon deploy
  - repl:
       - 
  - run:
       - 
  - eval:
       - 

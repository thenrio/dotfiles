name: clogl2
socket_name: clogl2
root: .
windows:
  - jam:
      layout: main-horizontal
      panes:
        - opt: 
          - ssh frclogrfidl2
          - cd /opt/jam
        - log:
          - ssh frclogrfidl2
          - cd /var/log/jam
          - tail -f jam.log
  - pickle:
      layout: main-horizontal
      panes:
        - tk:
          - ssh frclogrfidl2
          - cd /var/log/pickle
          - tail -f tk.log
        - tunnel:
          - ssh frclogrfidl2
          - cd /var/log/pickle
          - tail -f tunnel.log

name: clogl2
root: .
windows:
  - plug:
      layout: main-horizontal
      panes:
        - log: 
          - ssh frclogrfidl2
        - log:
          - ssh frclogrfidl2
          - cd /var/log/plug
  - pickle:
      layout: main-horizontal
      panes:
        - tk:
          - ssh frclogrfidl2
          - cd /var/log/pickle
          - tail -f tk.log
        - tunnel:
          - ssh frclogrfidl2
          - cd /var/log/pickle
          - tail -f tunnel.log

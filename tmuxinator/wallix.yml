name: wallix
socket_name: wallix
root: .
windows:
  - inspect:
      layout: main-horizontal
      panes:
        - etc:
          - cd /etc
          - sudo mv resolv.conf.ovpn.bak resolv.conf
        - ssh:
          - ssh wallix

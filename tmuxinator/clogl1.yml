name: clogl1
socket_name: clogl1
root: .
windows:
  - jam:
      layout: main-horizontal
      panes:
        - opt: 
          - ssh frclogrfidl1
          - cd /opt/jam
        - log:
          - ssh frclogrfidl1
          - cd /var/log/jam
          - tail -f jam.log
  - pickle:
      layout: main-horizontal
      panes:
        - tk:
          - ssh frclogrfidl1
          - cd /var/log/pickle
          - tail -f tk.log
        - tunnel:
          - ssh frclogrfidl1
          - cd /var/log/pickle
          - tail -f tunnel.log

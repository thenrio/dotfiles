#!/usr/bin/env bash
# vim: set ft=sh :
set -o errexit
set -o pipefail

usage() {
  commands=(prune run)
  for command in ${commands[*]}; do
    tm-$command --help
  done
}

command="$1"
case $command in
  --help)
    usage && exit 0
    ;;
  *)
    shift
    tm-$command "$@"
    ;;
esac
